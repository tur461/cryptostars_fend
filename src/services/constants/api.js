import { URL } from "./common";

const API_PATH = {
    TOKEN_LIST: URL.API_BACKEND_URL + '/api/get/tokenInfoList',
    PROJECT_VERSION: URL.API_BACKEND_URL + '/api/get/projectVersion',
}

export {
    API_PATH,
}