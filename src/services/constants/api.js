import { URL } from "./common";

const API_PATH = {
    TOKEN_LIST: URL.API_BACKEND_URL + '/api/get/tokenInfoList',
    F_END_VERSION: URL.API_BACKEND_URL + '/api/get/frontEndVersion',
}

export {
    API_PATH,
}